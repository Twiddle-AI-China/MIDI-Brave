{
  "name": "atlas-flow-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Atlas Flow \u2014 the Pad Top50 timbre map, running entirely on this machine",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "node prepack.js && electron-packager . AtlasFlow --platform=darwin --arch=arm64 --out=dist --overwrite --app-bundle-id=ai.twiddle.atlasflow --app-version=0.1.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^33.0.0"
  }
}
